import { Request, Response } from 'express';


export const validPermission = async (req: Request, res: Response) => {
    try {
        
    } catch (error) {
        
    }
}

export const permissionList = async (req: Request, res: Response) => {
    try {
        
    } catch (error) {
        
    }
}